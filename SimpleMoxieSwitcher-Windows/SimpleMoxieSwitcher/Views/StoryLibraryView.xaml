<UserControl x:Class="SimpleMoxieSwitcher.Views.StoryLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <Grid Background="#1A1A1A">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1A1A1A" Offset="0"/>
                <GradientStop Color="#4A2C8A" Offset="0.5"/>
                <GradientStop Color="#2A1B5A" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#33000000" Padding="30,20">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="📚 Story Library" FontSize="32" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Discover magical stories and adventures" FontSize="14"
                                 Foreground="#AAAAAA" Margin="0,5,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Create Story" Command="{Binding CreateStoryCommand}"
                              Style="{StaticResource AccentButtonStyle}" Padding="15,8" Margin="5,0"/>
                        <Button Content="Import" Command="{Binding ImportCommand}" Padding="15,8" Margin="5,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Filter Bar -->
            <Border Grid.Row="1" Background="#22000000" Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <RadioButton Content="All Stories" IsChecked="{Binding ShowAll}" Foreground="White" Margin="10,0"/>
                        <RadioButton Content="Favorites" IsChecked="{Binding ShowFavorites}" Foreground="White" Margin="10,0"/>
                        <RadioButton Content="Recent" IsChecked="{Binding ShowRecent}" Foreground="White" Margin="10,0"/>
                        <RadioButton Content="Custom" IsChecked="{Binding ShowCustom}" Foreground="White" Margin="10,0"/>
                    </StackPanel>
                    <Border Background="#33FFFFFF" CornerRadius="20" HorizontalAlignment="Right" Width="300">
                        <Grid>
                            <TextBlock Text="🔍" Margin="15,0,0,0" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                                   Background="Transparent" BorderThickness="0" Foreground="White"
                                   Margin="40,10,10,10"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- Story Grid -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Stories}" Margin="30">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.SelectStoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                  CommandParameter="{Binding}" Margin="15">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="#22FFFFFF" CornerRadius="15" Width="250" Height="350">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="200"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>

                                                <!-- Cover Image -->
                                                <Border Grid.Row="0" Background="{Binding CoverGradient}"
                                                      CornerRadius="15,15,0,0">
                                                    <Grid>
                                                        <TextBlock Text="{Binding CoverEmoji}" FontSize="64"
                                                                 HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        <Border Background="#AAFFDD00" CornerRadius="20" Padding="8,3"
                                                              HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10"
                                                              Visibility="{Binding IsNew, Converter={StaticResource BoolToVisibilityConverter}}">
                                                            <TextBlock Text="NEW" FontSize="10" FontWeight="Bold" Foreground="Black"/>
                                                        </Border>
                                                    </Grid>
                                                </Border>

                                                <!-- Story Info -->
                                                <StackPanel Grid.Row="1" Padding="15">
                                                    <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold"
                                                             Foreground="White" TextWrapping="Wrap" MaxHeight="50"/>
                                                    <TextBlock Text="{Binding Author}" FontSize="12"
                                                             Foreground="#AAAAAA" Margin="0,5,0,0"/>
                                                    <TextBlock Text="{Binding Description}" FontSize="11"
                                                             Foreground="#888888" TextWrapping="Wrap" MaxHeight="40"
                                                             Margin="0,10,0,0"/>

                                                    <Grid Margin="0,15,0,0">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="⏱️" FontSize="12" Margin="0,0,5,0"/>
                                                            <TextBlock Text="{Binding Duration}" FontSize="12" Foreground="#666666"/>
                                                            <TextBlock Text=" • " Foreground="#666666" Margin="5,0"/>
                                                            <TextBlock Text="👤" FontSize="12" Margin="0,0,5,0"/>
                                                            <TextBlock Text="{Binding AgeRange}" FontSize="12" Foreground="#666666"/>
                                                        </StackPanel>
                                                        <ItemsControl ItemsSource="{Binding Stars}" HorizontalAlignment="Right">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <StackPanel Orientation="Horizontal"/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <TextBlock Text="⭐" FontSize="12"/>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </Grid>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>